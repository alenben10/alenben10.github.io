---
import "../styles/global.css";
import profile from "../lib/profile";

interface Props {
  title: string;
  description: string;
}

const { title, description } = Astro.props;
const canonicalUrl = new URL(Astro.url.pathname, profile.siteUrl).toString();
const portraitUrl = new URL(profile.portraitImage, profile.siteUrl).toString();
const socialImageUrl = new URL("/og.png", profile.siteUrl).toString();
const nameKeywords = [profile.fullName, ...profile.alternateNames].join(", ");
const structuredData = {
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "Person",
      "@id": `${profile.siteUrl}/#person`,
      name: profile.fullName,
      givenName: "Alen",
      familyName: "Zimic-Sheen",
      additionalName: "Zimic Sheen",
      alternateName: profile.alternateNames,
      url: profile.siteUrl,
      image: portraitUrl,
      email: `mailto:${profile.email}`,
      jobTitle: "Laboratory Technician",
      sameAs: [profile.githubUrl, profile.linkedInUrl],
      affiliation: [
        {
          "@type": "CollegeOrUniversity",
          name: "Cornell University",
        },
        {
          "@type": "Organization",
          name: "Moghe Lab",
        },
      ],
      knowsAbout: [
        "Computational biology",
        "Bioinformatics",
        "Biological data science",
        "Machine learning",
        "Omics data analysis",
        "Plant-microbe interactions",
        "Environmental biotechnology",
      ],
    },
    {
      "@type": "WebSite",
      "@id": `${profile.siteUrl}/#website`,
      name: `${profile.fullName} portfolio`,
      alternateName: profile.alternateNames,
      url: profile.siteUrl,
      author: {
        "@id": `${profile.siteUrl}/#person`,
      },
      inLanguage: "en",
    },
  ],
};
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content={description} />
    <meta name="author" content={profile.fullName} />
    <meta
      name="keywords"
      content={`${nameKeywords}, computational biology, bioinformatics, biological data science, Cornell University`}
    />
    <meta name="robots" content="index, follow" />
    <meta
      name="google-site-verification"
      content="SG10dQM62fAg4iT5IRWIfZ_8J4eu1l--00fpeox_0xk"
    />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="theme-color" content="#080b10" />
    <link rel="canonical" href={canonicalUrl} />
    <meta property="og:type" content="website" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonicalUrl} />
    <meta property="og:image" content={socialImageUrl} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:image:alt" content={`${profile.fullName} — computational biology and bioinformatics.`} />
    <meta property="og:site_name" content={`${profile.fullName} portfolio`} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={socialImageUrl} />
    <meta name="twitter:image:alt" content={`${profile.fullName} — computational biology and bioinformatics.`} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;600;700;800&display=swap"
      rel="stylesheet"
    />
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <title>{title}</title>
    <script type="application/ld+json" set:html={JSON.stringify(structuredData)}></script>
  </head>
  <body>
    <slot />
  </body>
</html>
